services:

# ----------- main pipeline ----------------#
  pipeline:
    build: .
    container_name: pipeline
    env_file:
      - .env
    ports:
      - "8001:8001"
    # depends_on:
    #   - kafka
    #   - clickhouse
    volumes:
      - ./log_data:/app/log_data
    restart: unless-stopped
    stop_grace_period: 5s

